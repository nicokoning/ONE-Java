package one.java.events;

/**
 *
 * @author Nico
 */
public interface ONEChangeListener
{
    /**
     * Called when there is a change to a one object
     */ 
    public void onChange(ONEChangeEvent event);
}
